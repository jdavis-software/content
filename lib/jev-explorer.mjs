import {escapeHtml as e} from './markdown.mjs';
import {cases,routes,assess} from '../site/jev-model.js';
export const JEV_ANCHOR='<h2 id="browser-actions-make-the-separation-visible">';
export function jevExplorer(){
 const a=assess();
 return `<section id="jev-decision-explorer" class="jev-explorer" aria-labelledby="jev-explorer-title"><div class="jev-eyebrow">INTERACTIVE EXPLAINER · NO LIVE MODEL</div><h2 id="jev-explorer-title">Choose a route.<br><span>Keep the boundaries.</span></h2><p class="jev-intro">Change the task and see the proposed route. Then change the conditions the software must check.</p><fieldset class="jev-cases" disabled><legend>1 / Observed task</legend>${cases.map((c,i)=>`<button type="button" data-jev-case="${c.id}" aria-pressed="${i===0}">${e(c.label)}</button>`).join('')}</fieldset><p class="jev-state" data-jev-state>${e(a.scenario.state)}</p><div class="jev-network" aria-label="Observed state feeds a bounded decision; software gates the selected route"><div class="jev-source"><small>OBSERVATION</small><strong>Current state</strong><span>Evidence + eligible menu</span></div><div class="jev-decision"><small>BOUNDED JUDGMENT</small><strong>Jev-style decision</strong><span>Authored probabilities</span></div><div class="jev-routes">${routes.map((r,i)=>`<div class="jev-route" data-jev-route="${r.id}" data-selected="${i===0}"><div><strong>${e(r.label)}</strong><span data-jev-prob>${a.scenario.probabilities[i].toFixed(2)}</span></div><div class="jev-track" aria-hidden="true"><span data-jev-bar style="width:${a.scenario.probabilities[i]*100}%"></span></div><small>${e(r.detail)}</small></div>`).join('')}</div></div><fieldset class="jev-gates" disabled><legend>2 / Independent software conditions</legend><label><input type="checkbox" data-jev-flag="fresh" checked> Observation is current</label><label><input type="checkbox" data-jev-flag="allowed" checked> Execution is permitted</label><label><input type="checkbox" data-jev-flag="verified" checked> Result check passes</label></fieldset><div class="jev-outcome" data-jev-status="${a.status}" role="status" aria-live="polite" aria-atomic="true"><small data-jev-path>PROPOSED: ${e(a.route.label)} · CHECKED PATH</small><h3 data-jev-title>${e(a.title)}</h3><p data-jev-detail>${e(a.detail)}</p></div><div class="jev-actions"><button type="button" data-jev-trace disabled>Trace this route <span aria-hidden="true">→</span></button><button type="button" data-jev-reset disabled>Reset example</button><span data-jev-trace-status role="status"></span></div><p class="jev-disclaimer">Illustration only. Probabilities are authored, not returned by Jev. The 0.70 selection rule is not Jev’s confidence statistic. These controls grant no real permissions and execute nothing.</p><noscript><p>The static example above shows one route. Interactive controls require JavaScript; the full article and explanation remain readable.</p></noscript></section>`;
}
export function insertJevExplorer(html){if(!html.includes(JEV_ANCHOR))throw new Error('Missing Jev article insertion heading');return html.replace(JEV_ANCHOR,jevExplorer()+JEV_ANCHOR);}
