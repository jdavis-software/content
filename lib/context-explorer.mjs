import {escapeHtml as e} from './markdown.mjs';
import {MODES,makePacket,inspectPacket} from '../site/context-model.js';
const STATE={current:'Current',optional:'Additional',omitted:'Not selected',missing:'Missing',stale:'Stale',blocked:'Access withdrawn'};
export function contextExplorer(){
 const result=inspectPacket(makePacket());
 return `<section class="context-explorer" id="context-packet-explorer" aria-labelledby="context-title"><div class="ce-heading"><span class="small-meta">Interactive evidence check · fictional task</span><h2 id="context-title">More context. Better evidence?</h2><p>Task: stop retrying validation failures. Inspect what the agent receives—not just how much it receives.</p></div><div class="ce-presets" role="group" aria-label="Context packet scenario">${Object.entries(MODES).map(([key,label])=>`<button type="button" data-context-mode="${key}" aria-pressed="${key==='focused'}">${e(label)}</button>`).join('')}</div><div class="ce-controls"><label>Required contract <select id="context-revision" aria-label="Required contract revision"><option value="r2">Revision 2</option><option value="r3">Revision 3</option></select></label><label class="ce-access"><input id="context-access" type="checkbox"> Simulate revoked contract access</label><button type="button" id="context-refresh">Refresh packet</button></div><div class="ce-metrics"><div><strong data-ce-count="current">5 / 5</strong><span>Required items current</span></div><div><strong data-ce-count="selected">5</strong><span>Selected evidence items</span></div><div><strong data-ce-count="optional">0</strong><span>Additional items</span></div></div><div class="ce-result" role="status" aria-live="polite" aria-atomic="true"><strong id="context-verdict">${e(result.headline)}</strong><p id="context-guidance">${e(result.guidance)}</p></div><div class="ce-inspection"><div class="ce-sources" role="group" aria-label="Inspect evidence">${result.rows.map(row=>`<button type="button" class="ce-source" data-context-source="${row.id}" data-state="${row.state}" aria-pressed="${row.id==='contract'}"><span>${e(row.label)}<small>${e(row.path)}</small></span><em>${STATE[row.state]}</em></button>`).join('')}</div><div class="ce-detail"><span class="small-meta">Selected evidence</span><h3 id="context-detail-title">Accepted error contract</h3><dl><dt>Source</dt><dd id="context-detail-path">api/errors.yaml</dd><dt>Packet revision</dt><dd id="context-detail-revision">r2</dd><dt>Required revision</dt><dd id="context-detail-expected">r2</dd></dl><p id="context-detail-body">${e(result.rows.find(r=>r.id==='contract').body)}</p></div></div><p class="ce-note">Counts are fictional evidence items—not tokens or measured model quality. Requirements are manually declared. This static demo contains no private data and does not enforce real access control.</p><noscript><p class="ce-note">JavaScript is off. The complete focused-packet example remains visible; scenario controls need JavaScript.</p></noscript></section>`;
}
export function insertContextExplorer(html){const anchor='<h2 id="choose-retrieval-by-the-question">';if(!html.includes(anchor))throw new Error('Context explorer article anchor missing');return html.replace(anchor,contextExplorer()+anchor);}
