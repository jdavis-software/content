import {escapeHtml as e} from './markdown.mjs';
import {NODES, SCENARIOS, STATE, evaluate} from '../site/devops-model.js';
export const DEVOPS_ANCHOR='<h2 id="postgresql-belongs-in-the-architecture-not-in-a-miscellaneous-services-box">';
export function devopsFlow() {
  const model=evaluate();
  const node=n=>`<button type="button" class="dv-node" data-dv-node="${n.id}" data-state="${model.states[n.id]}" aria-pressed="false"><span class="dv-kind">${n.group==='input'?'◈ Canonical':n.group==='generated'?'Generated · do not hand-edit':'Build → runtime'}</span><strong>${e(n.name)}</strong><span class="dv-sub">${e(n.sub)}</span><span class="dv-status">${n.group==='input'?(model.states[n.id]==='changed'?'● Changed input':'Source of truth'):`${STATE[model.states[n.id]].symbol} ${STATE[model.states[n.id]].label}`}</span></button>`;
  const row=(group,label)=>`<div class="dv-row-label">${label}</div><div class="dv-row dv-${group}">${NODES.filter(n=>n.group===group).map(node).join('')}</div>`;
  return `<section class="devops-flow" id="devops-invalidation" aria-labelledby="devops-title"><header class="dv-header"><span class="dv-eyebrow">02 / THE DELIVERY SYSTEM</span><h2 id="devops-title">Reuse the work.<br><em>Not the state.</em></h2><p>Choose a change. Follow its dependencies, see what stays reusable, and keep runtime data out of the build-cache story.</p></header><div class="dv-controls" hidden><fieldset><legend>What changed?</legend><div class="dv-scenarios">${Object.entries(SCENARIOS).map(([key,s])=>`<button type="button" data-dv-scenario="${key}" aria-pressed="${key==='ui'}">${e(s.label)}</button>`).join('')}</div></fieldset><div class="dv-mode-row"><fieldset><legend>Available build cache</legend><div class="dv-modes"><button type="button" data-dv-mode="warm" aria-pressed="true">Warm</button><button type="button" data-dv-mode="cold" aria-pressed="false">Cold</button></div></fieldset><button type="button" class="dv-trace" data-dv-trace>Trace dependencies ↘</button></div></div><p class="dv-static-note">Default example: a UI edit with warm build caches. Enable JavaScript to explore the other change scenarios.</p><div class="dv-legend"><span data-state="reuse">✓ Reuse</span><span data-state="rebuild">↻ Rebuild</span><span data-state="check">◇ Fresh check</span><span data-state="cold">＋ Cold miss</span><span data-state="outside">— Not selected</span></div><div class="dv-story"><h3 data-dv-headline>${e(model.plan.headline)}</h3><p data-dv-explanation>${e(model.plan.text)}</p><p class="dv-counts" data-dv-counts aria-live="polite" aria-atomic="true">1 rebuild · 1 reusable result · 1 fresh check</p></div><div class="dv-map"><svg class="dv-edges" aria-hidden="true" focusable="false"></svg>${row('input','01 / Edit the source of truth')}${row('generated','02 / Derive reproducible code')}${row('artifact','03 / Build only the selected targets')}</div><p class="dv-path-note">Lines show selected generation dependencies. Source, toolchain, and relevant configuration also feed the builds. Select a box for its role.</p><div class="dv-detail" hidden role="status"><strong data-dv-detail-title></strong><p data-dv-detail-text></p></div><div class="dv-cache-title">Warm stores ≠ one global cache</div><div class="dv-cache-grid">${[['pnpm','pnpm','Package downloads'],['go','Go','Modules / compile'],['nx','Nx','Task results'],['docker','Docker BuildKit','Layers / cache mounts']].map(([id,title,sub])=>`<div><strong>${title}</strong><span>${sub}</span><small data-dv-cache="${id}">${e(model.caches[id])}</small></div>`).join('')}</div><div class="dv-checks"><strong>◇ Run the relevant checks</strong><div data-dv-checks>${model.checks.map(c=>`<span>${e(c)}</span>`).join('')}</div><p>Reusing an artifact does not approve a release. These behavior checks are deliberately separate from cached build results.</p></div><div class="dv-runtime"><h3>Runtime state has its own boundaries.</h3><div class="dv-stores"><div><span class="dv-kind">AUTHORITATIVE APPLICATION DATA</span><strong>PostgreSQL</strong><p>Go API + Activities via pgx/sqlc. <br>Tern owns migration application.</p><small data-dv-runtime="application">${e(model.runtime.application)}</small></div><div><span class="dv-kind">DURABLE WORKFLOW STATE</span><strong>Temporal persistence</strong><p>Owned by Temporal Service. <br>Workers connect to the service.</p><small data-dv-runtime="temporal">History preserved</small></div><div><span class="dv-kind">DERIVED / NON-AUTHORITATIVE</span><strong>Optional search index</strong><p>For example, SQLite FTS. <br>Rebuild only from its own inputs.</p><small data-dv-runtime="derived">Optional · not provisioned</small></div></div></div><div class="dv-release">Relevant checks <span>→</span> approve identified artifacts <span>→</span> deploy affected services</div><details class="dv-notes"><summary>Model assumptions & primary references</summary><p>This is an authored explanation, not live infrastructure, a benchmark, or an instruction to flush caches. Warm assumes matching unchanged entries exist; cold removes build-cache availability, not canonical files or database volumes. Identical regenerated outputs may preserve downstream cache hits. Runtime/data-cache invalidation is a separate subject.</p><p>SQL here means a schema change affecting selected queries; Workflow means deterministic Workflow-code changes, not every Activity edit. Shared dependencies and deployment configuration can widen the true scope. Isolate mutable per-worktree outputs and test databases; do not share writable PostgreSQL volumes between servers. No service, database, migration, or deployment is executed by this diagram.</p><p><a href="https://nx.dev/docs/reference/inputs">Nx inputs</a> · <a href="https://docs.docker.com/build/cache/optimize/">Docker caches</a> · <a href="https://docs.sqlc.dev/en/latest/tutorials/getting-started-postgresql.html">sqlc inputs</a> · <a href="https://docs.temporal.io/develop/go/workflows/versioning">Temporal versioning</a></p></details></section>`;
}
export function insertDevops(html) {
  if (!html.includes(DEVOPS_ANCHOR)) throw new Error('DevOps insertion anchor is missing; review article placement');
  return html.replace(DEVOPS_ANCHOR,devopsFlow()+DEVOPS_ANCHOR);
}
