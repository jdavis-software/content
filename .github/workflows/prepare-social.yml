name: Prepare social preview source
on:
  push:
    branches: [feature/social-preview-image]
    paths: ['.github/workflows/prepare-social.yml']
permissions:
  contents: read
jobs:
  snapshot:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - run: git archive --format=zip HEAD -o /tmp/social-source.zip
      - uses: actions/upload-artifact@v4
        with:
          name: social-source
          path: /tmp/social-source.zip
          retention-days: 2
